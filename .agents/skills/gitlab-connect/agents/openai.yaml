interface:
  display_name: "GitLab Connect"
  short_description: "Conecta y opera GitLab API con credenciales .env."
  default_prompt: "Use this skill to connect to GitLab using .env credentials, validate access, and fetch project and merge request data reproducibly."
